## skyspy airband

RTL-Airband Recording Uploader

### Synopsis

RTL-Airband Recording Uploader

Watches a directory for MP3 recordings from rtl-airband, maps frequencies
to channel labels, filters empty/short transmissions, and uploads them
to the SkySpy API.

Runs as a headless daemon by default. Use --tui for a live monitoring display.

Examples:
  skyspy airband --dir /recordings
  skyspy airband --dir /recordings --tui
  skyspy airband --dir /recordings --freq-map /etc/skyspy/freq-map.json
  skyspy airband --dir /recordings --dry-run
  skyspy airband --dir /recordings --host server.local --port 8000 --api-key sk_xxx

```
skyspy airband [flags]
```

### Options

```
      --dir string              Recordings directory (required, or SKYSPY_RECORDINGS_DIR)
      --dry-run                 Parse and filter without uploading
      --freq-map string         Path to JSON frequency map file (or SKYSPY_FREQ_MAP)
  -h, --help                    help for airband
      --max-retries int         Maximum upload retries (default 3)
      --metrics-port int        Prometheus metrics port, 0 to disable (default 9090)
      --min-duration float      Minimum duration in seconds (default 2.0)
      --min-file-size int       Minimum file size in bytes (default 2048)
      --poll-interval int       Poll interval in seconds (default 5)
      --stability-seconds int   File stability wait in seconds (default 2)
      --tui                     Enable live monitoring TUI
      --upload-timeout int      Upload timeout in seconds (default 60)
```

### Options inherited from parent commands

```
      --host string   Server hostname
      --port int      Server port
```

### SEE ALSO

* [skyspy](skyspy.md)	 - SkySpy Radar Pro - Full-Featured Aircraft Display

###### Auto generated by spf13/cobra on 15-Jul-2026
