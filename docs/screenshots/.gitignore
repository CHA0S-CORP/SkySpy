# Documentation screenshots
# Generated by `npm run docs:generate` from web/.
#
# These assets are committed (they are referenced by README.md and the docs
# guides, so they must be tracked to render on GitHub). Regenerate and commit
# the diff whenever the UI changes.
#
# Ignore only Playwright's transient run artifacts, never the PNG/GIF captures.
report/
output/
